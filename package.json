{
  "name": "bhuvansa-monorepo",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --filter web dev",
    "build": "bun run --filter web build",
    "lint": "bun run --filter web lint",
    "start": "bun run --filter web start"
  }
}
